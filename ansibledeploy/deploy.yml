---
- name: Deploy Docker image and Kubernetes resources for spemajorf
  hosts: localhost
  remote_user: subbhashit
  become: false
  tasks:
    - name: Run Kubernetes Deployment files
      shell: kubectl apply -f "{{ item }}" --namespace=your-namespace --serviceaccount=fork8s
      with_fileglob:
        - ../kubernetes/*.yaml
