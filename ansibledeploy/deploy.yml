---
- name: Deploy Docker image and Kubernetes resources for spemajorf
  hosts: localhost
  remote_user: subbhashit
  become: false
  tasks:
    - name: Run Kubernetes Deployment files
      shell: kubectl apply -f "{{ item }}" {% if kubectl_validate == false %}--validate=false{% endif %}
      with_fileglob:
        - ../kubernetes/*.yaml
